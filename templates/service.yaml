apiVersion: v1
kind: Service
metadata:
  name: {{ include "web-it-ommey.fullname" . }}
  labels:
    {{- include "web-it-ommey.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "web-it-ommey.selectorLabels" . | nindent 4 }}
